[project]
name = "Hydra-Game"
version = "1.1"
description = "Terminal entrypoint for Hydra"

[project.scripts]
Hydra = "Hydra:main"

[tool.setuptools]
py-modules = ["Hydra", "MacAttack"]

[tool.setuptools.packages.find]
where = ["."]
include = ["Game"]