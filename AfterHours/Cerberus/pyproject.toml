[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "Cerberus-Game"
version = "2.4"
description = "The BonePile"
requires-python = ">=3.10"
dependencies = [
    "cryptography",
]

[project.scripts]
Cerberus = "Cerberus:main"

[tool.setuptools]
py-modules = ["Cerberus", "DevilDog", "LuckyDog"]

[tool.setuptools.packages.find]
where = ["."]
include = ["Game*"]
