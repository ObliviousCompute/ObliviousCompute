[project]
name = "Hydra-Game"
version = "1.2"
description = "SwampThing"

[project.scripts]
Hydra = "Hydra:main"

[tool.setuptools]
py-modules = ["Hydra", "MacAttack"]

[tool.setuptools.packages.find]
where = ["."]
include = ["Game"]